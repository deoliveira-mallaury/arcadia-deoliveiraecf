{% extends 'base.html.twig' %}

{% block title %}Animal
{% endblock %}

{% block body %}
	<turbo-frame id="show_animal">
		<legend class="d-flex justify-content-between align-items-center">{{animal.name}}
			<button data-bs-toggle="collapse" href="#show_animal">
				<i class="bi bi-x-circle"></i>
			</button>
		</legend>
		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ animal.id }}</td>
				</tr>
				<tr>
					<th>Nom</th>
					<td>{{ animal.name }}</td>
				</tr>
				<tr>
					<th>Condition</th>
					<td>{{ animal.etat }}</td>
				</tr>
				<tr>
					<th>Quantité nourriture</th>
					<td>{{ animal.foodQty }}</td>
				</tr>
			</tbody>
		</table>
	</turbo-frame>

	<a href="{{ path('app_animal_index') }}">back to list</a>

	<a href="{{ path('app_animal_edit', {'id': animal.id}) }}">edit</a>

	{{ include('animal/_delete_form.html.twig') }}
{% endblock %}
