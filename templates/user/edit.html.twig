{% extends 'base.html.twig' %}

{% block title %}Edit User
{% endblock %}

{% block body %}
	<h1>Edit User</h1>
	<turbo-frame id="edit_user">
		<legend class="d-flex justify-content-between align-items-center">Modifier Utilisateur
			<button data-bs-toggle="collapse" href="#edit_user">
				<i class="bi bi-x-circle"></i>
			</button>
		</legend>
		{{ form_start(form) }}
		{{ form_widget(form) }}
		<button class="btn">{{ button_label|default('Update') }}</button>
		{{ form_end(form) }}
	</turbo-frame>

	<a href="{{ path('app_user_index') }}">back to list</a>

	{{ include('user/_delete_form.html.twig') }}
{% endblock %}
