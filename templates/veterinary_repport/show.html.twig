{% extends 'base.html.twig' %}

{% block title %}VeterinaryRepport
{% endblock %}

{% block body %}
	<h1>VeterinaryRepport</h1>
	<turbo-frame id="show_VetRepports">

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ veterinary_repport.id }}</td>
				</tr>
				<tr>
					<th>Date</th>
					<td>{{ veterinary_repport.date ? veterinary_repport.date|date('Y-m-d') : '' }}</td>
				</tr>
				<tr>
					<th>Detail</th>
					<td>{{ veterinary_repport.detail }}</td>
				</tr>
			</tbody>
		</table>
	</turbo-frame>


	<a href="{{ path('app_veterinary_repport_index') }}">back to list</a>

	<a href="{{ path('app_veterinary_repport_edit', {'id': veterinary_repport.id}) }}">edit</a>

	{{ include('veterinary_repport/_delete_form.html.twig') }}
{% endblock %}
